{
  "name": "coverage-report-transform",
  "version": "1.8.1",
  "description": "Collect the list of packages from changeset folder",
  "main": "index.js",
  "bin": {
    "coverage-report-transform": "index.js"
  },
  "scripts": {
    "patch": "npm version patch && npm publish && git push --follow-tags",
    "minor": "npm version minor && npm publish && git push --follow-tags",
    "major": "npm version major && npm publish && git push --follow-tags"
  },
  "keywords": [
    "changeset"
  ],
  "author": "Anton Nemtsev <newsilentimp@gmail.com>",
  "license": "ISC",
  "dependencies": {
    "commander": "^11.0.0",
    "markdown-table": "^3.0.4",
    "vite": "^6.3.5"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "ts-node": "^10.9.2",
    "tsx": "^4.19.4",
    "typescript": "^5.8.3"
  }
}
